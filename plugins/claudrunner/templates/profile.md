# Project profile

<!-- Written once by /claudrunner:init from reading this repository, so that no run has to
     rediscover it. Every session reads the first 4 KB: keep it short, specific and true.
     Point at files instead of copying them. Re-run init, or edit by hand, when it drifts.
     profiled at: <commit sha>, <date> -->

## Stack

<!-- Languages and versions, frameworks, the database and other services, package
     managers. One line each. Example: "PHP 8.3, Laravel 12, MySQL 8.4 (not SQLite)". -->

## Where things live

<!-- The map a newcomer needs: where features, tests, migrations, config and the frontend
     are. Name the directories; say which ones are generated or vendored and never edited. -->

## How code is written here

<!-- The conventions a reviewer would reject a change for breaking, as found in the code,
     not generic advice: naming, where logic goes (services, actions, hooks...), error
     handling, how validation and authorization are done. Name one or two existing files
     that are good examples to imitate. -->

## How to test a change

<!-- The commands are in config.yml. Here: what a good test looks like here (name an
     example test file), what the tests need (database, services), which suites are slow,
     and anything that makes a test pass locally but fail in CI. -->

## What must not break

<!-- Deploy and release facts (for example "a merge to main deploys to production"),
     privacy or tenancy rules, scale-sensitive tables, generated files, areas that are
     deliberately unconventional. Plus the failure modes typical of this stack. -->

## Rules that live elsewhere

<!-- Pointers only: AGENTS.md, CLAUDE.md, CONTRIBUTING.md, docs/... Say which one wins on a
     conflict. Never copy their content here. -->
